# Fase 1D — GDPR base (cookie banner + privacy/cookie) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Predisporre la conformità GDPR di base dell'MVP: cookie banner con gestione consensi che **blocca gli embed YouTube** fino al consenso, pagine Privacy/Cookie (bozza IT reale), link nel footer. Chiude la Fase 1.

**Architecture:** Il consenso vive in un **cookie first-party** `mcm_consent` (no DB), letto **lato server** nel root layout e idratato in un `ConsentProvider` (client) per evitare flash. Un `CookieBanner` legge/scrive il consenso; un `VideoYouTube` gate mostra un placeholder al posto dell'iframe finché `external` non è vero. Due pagine statiche pubbliche `/privacy` e `/cookie`.

**Tech Stack:** Next.js **16** (App Router, `cookies()` async da `next/headers`, server+client components), next-intl (IT), React context, Tailwind con token di tema, vitest (logica pura).

## Global Constraints

- **Next.js 16** (non 15): prima di toccare API Next consultare `node_modules/next/dist/docs/`. `cookies()` è **async** (`await cookies()`). Convenzione `proxy.ts`.
- **Nessun DB, nessuna migrazione, nessuna RLS.** Il consenso è solo un cookie client.
- **Cookie `mcm_consent`**: valore = `encodeURIComponent(JSON)` dove il JSON è `{v:1, external:bool, ts:number}`. Attributi: `path=/`, `max-age=31536000`, `SameSite=Lax`, **non** HttpOnly. Le funzioni pure `parseConsent`/`serializeConsent` operano sul **JSON decodificato**; l'encode/decode di trasporto è ai due call-site (client write, server read).
- **Solo token di tema** per i colori; **nessuna stringa UI hardcoded** → tutto via next-intl (namespace `consent`, `privacy`, `cookiePolicy`, voci in `footer`).
- **GDPR**: "Rifiuta" prominente quanto "Accetta"; banner **non bloccante** (niente cookie wall); revoca facile come il consenso (link footer "Preferenze cookie").
- **Consenso letto SSR** → `bannerAperto` e il gate video derivano entrambi da `initialConsent` al primo paint (no hydration mismatch, no flash).
- **Un commit per task** (dopo review). **Fermarsi e chiedere conferma dopo ogni task.**
- **Branch:** `feat/fase1d-gdpr` (parte da `main`).
- ⚠️ **Non lanciare `npm run build` mentre gira `next dev`.**
- **Spec di riferimento:** `docs/superpowers/specs/2026-07-28-fase1d-gdpr-design.md`.
- Le policy sono **bozza non validata da un legale** (disclaimer in cima); dati del Titolare e trasferimenti extra-UE = **placeholder marcati**.

---

## File Structure

**Nuovi:**
- `src/lib/consent/consenso.ts` — logica pura + costanti.
- `src/lib/consent/consenso.test.ts` — test vitest.
- `src/components/features/consent/ConsentProvider.tsx` — context (client) + `useConsent()`.
- `src/components/features/consent/CookieBanner.tsx` — banner (client).
- `src/components/features/consent/VideoYouTube.tsx` — gate embed (client).
- `src/components/features/consent/PreferenzeCookieButton.tsx` — bottone footer (client).
- `src/app/[locale]/(public)/privacy/page.tsx` — Privacy Policy (server).
- `src/app/[locale]/(public)/cookie/page.tsx` — Cookie Policy (server).

**Modificati:**
- `src/app/[locale]/layout.tsx` — legge cookie server-side, monta `ConsentProvider` + `CookieBanner`.
- `src/components/layout/Footer.tsx` — link Privacy · Cookie · Preferenze cookie.
- `src/components/features/events/GalleryEvento.tsx` — usa `<VideoYouTube>` al posto dell'iframe inline.
- `src/messages/it.json` — namespace `consent`, `privacy`, `cookiePolicy` + voci `footer`.

---

## Task 0: Setup branch

- [ ] **Step 1: Creare il branch di fase**

Run:
```bash
git checkout main
git checkout -b feat/fase1d-gdpr
git log --oneline -1
```

Nessun commit in questo task.

---

## Task 1: Logica pura del consenso (`consenso.ts`) + vitest

**Files:**
- Create: `src/lib/consent/consenso.ts`
- Test: `src/lib/consent/consenso.test.ts`

**Interfaces:**
- Consumes: nulla.
- Produces: `type Consent = { external: boolean; ts: number }`; costanti `CONSENT_COOKIE`, `CONSENT_VERSION`, `CONSENT_MAX_AGE`; `parseConsent(cookieValue: string | undefined | null): Consent | null`; `serializeConsent(consent: Consent): string`.

- [ ] **Step 1: Scrivere i test (falliscono)**

Create `src/lib/consent/consenso.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { parseConsent, serializeConsent, CONSENT_VERSION } from "./consenso";

describe("parseConsent", () => {
  it("valore assente → null", () => {
    expect(parseConsent(undefined)).toBeNull();
    expect(parseConsent(null)).toBeNull();
    expect(parseConsent("")).toBeNull();
  });
  it("JSON invalido → null", () => {
    expect(parseConsent("non-json")).toBeNull();
    expect(parseConsent("{broken")).toBeNull();
  });
  it("non-oggetto → null", () => {
    expect(parseConsent("42")).toBeNull();
    expect(parseConsent("null")).toBeNull();
    expect(parseConsent('"stringa"')).toBeNull();
  });
  it("versione diversa → null", () => {
    expect(parseConsent(JSON.stringify({ v: 999, external: true, ts: 1 }))).toBeNull();
  });
  it("external mancante o non booleano → null", () => {
    expect(parseConsent(JSON.stringify({ v: CONSENT_VERSION, ts: 1 }))).toBeNull();
    expect(parseConsent(JSON.stringify({ v: CONSENT_VERSION, external: "si", ts: 1 }))).toBeNull();
  });
  it("oggetto valido → Consent", () => {
    expect(parseConsent(JSON.stringify({ v: CONSENT_VERSION, external: true, ts: 123 }))).toEqual({
      external: true,
      ts: 123,
    });
    expect(parseConsent(JSON.stringify({ v: CONSENT_VERSION, external: false, ts: 0 }))).toEqual({
      external: false,
      ts: 0,
    });
  });
  it("ts mancante o non numerico → 0 (ma resta valido se external ok)", () => {
    expect(parseConsent(JSON.stringify({ v: CONSENT_VERSION, external: true }))).toEqual({
      external: true,
      ts: 0,
    });
  });
});

describe("serializeConsent", () => {
  it("produce JSON con la versione corrente", () => {
    const s = serializeConsent({ external: true, ts: 123 });
    expect(JSON.parse(s)).toEqual({ v: CONSENT_VERSION, external: true, ts: 123 });
  });
  it("round-trip conserva external e ts", () => {
    const c = { external: false, ts: 987654 };
    expect(parseConsent(serializeConsent(c))).toEqual(c);
  });
});
```

- [ ] **Step 2: Eseguire i test (devono fallire)**

Run:
```bash
npm test -- consenso
```
Expected: FAIL (modulo/funzioni non trovati).

- [ ] **Step 3: Implementare la logica pura**

Create `src/lib/consent/consenso.ts`:
```ts
// Consenso ai cookie/contenuti di terze parti. Logica pura (nessun I/O): opera sul
// JSON DECODIFICATO del cookie; l'encode/decode di trasporto (encodeURIComponent lato
// client, decodeURIComponent lato server) sta ai due call-site. È l'unico pezzo con
// logica non banale della fase 1D → coperto da test (vitest).

export type Consent = { external: boolean; ts: number };

export const CONSENT_COOKIE = "mcm_consent";
export const CONSENT_VERSION = 1;
export const CONSENT_MAX_AGE = 31536000; // 1 anno in secondi

type Stored = { v: number; external: boolean; ts: number };

/**
 * Ritorna il consenso o null se il valore manca / è malformato / è di una versione
 * ignota (→ "non ha ancora scelto", si ri-chiede). Non lancia mai.
 */
export function parseConsent(cookieValue: string | undefined | null): Consent | null {
  if (!cookieValue) return null;
  let obj: unknown;
  try {
    obj = JSON.parse(cookieValue);
  } catch {
    return null;
  }
  if (typeof obj !== "object" || obj === null) return null;
  const o = obj as Record<string, unknown>;
  if (o.v !== CONSENT_VERSION) return null;
  if (typeof o.external !== "boolean") return null;
  const ts = typeof o.ts === "number" ? o.ts : 0;
  return { external: o.external, ts };
}

/** Serializza il consenso nel JSON del cookie (senza gli attributi di trasporto). */
export function serializeConsent(consent: Consent): string {
  const stored: Stored = { v: CONSENT_VERSION, external: consent.external, ts: consent.ts };
  return JSON.stringify(stored);
}
```

- [ ] **Step 4: Eseguire i test (devono passare)**

Run:
```bash
npm test -- consenso
```
Expected: PASS.

- [ ] **Step 5: Verifiche + commit**

Run:
```bash
npx tsc --noEmit && npm run lint
git add src/lib/consent/consenso.ts src/lib/consent/consenso.test.ts
git commit -m "feat(1d): logica pura del consenso (parse/serialize) + test"
```

---

## Task 2: ConsentProvider + CookieBanner + wiring nel layout + i18n

**Files:**
- Create: `src/components/features/consent/ConsentProvider.tsx`
- Create: `src/components/features/consent/CookieBanner.tsx`
- Modify: `src/app/[locale]/layout.tsx`
- Modify: `src/messages/it.json`

**Interfaces:**
- Consumes: `Consent`, `CONSENT_COOKIE`, `CONSENT_MAX_AGE`, `serializeConsent`, `parseConsent` (Task 1).
- Produces: `ConsentProvider` (default export) + `useConsent()` hook returning `{ consent, haScelto, bannerAperto, external, acceptAll, rejectAll, setExternal, riapriBanner }`; `CookieBanner` (default export). Consumed by Task 3 (`VideoYouTube`) and Task 5 (`PreferenzeCookieButton`).

- [ ] **Step 1: Aggiungere il namespace i18n `consent` + voci footer**

Modify `src/messages/it.json`. Aggiungere il blocco `consent` (dopo `gallery`, con la virgola prima) e le nuove voci in `footer`:
```json
  "consent": {
    "bannerTitle": "Rispettiamo la tua privacy",
    "bannerText": "Usiamo cookie tecnici necessari al funzionamento del sito. I contenuti di terze parti (es. i video di YouTube negli album) vengono caricati solo con il tuo consenso.",
    "acceptAll": "Accetta tutti",
    "rejectAll": "Rifiuta non essenziali",
    "customize": "Personalizza",
    "savePrefs": "Salva preferenze",
    "catNecessary": "Necessari",
    "catNecessaryDesc": "Indispensabili: sessione di accesso, sicurezza anti-bot, memorizzazione di questa preferenza. Sempre attivi.",
    "alwaysOn": "Sempre attivi",
    "catExternal": "Contenuti di terze parti",
    "catExternalDesc": "Permette di caricare i video di YouTube incorporati nelle pagine degli eventi. Senza consenso restano bloccati.",
    "linkPrivacy": "Privacy Policy",
    "linkCookie": "Cookie Policy",
    "videoBlocked": "Video di YouTube bloccato: richiede il consenso ai contenuti di terze parti.",
    "enableExternal": "Attiva contenuti esterni",
    "watchOnYoutube": "Guarda su YouTube"
  }
```
E dentro il blocco `"footer"` esistente, aggiungere le chiavi:
```json
    "privacy": "Privacy",
    "cookie": "Cookie",
    "cookiePrefs": "Preferenze cookie"
```

- [ ] **Step 2: Scrivere il ConsentProvider**

Create `src/components/features/consent/ConsentProvider.tsx`:
```tsx
"use client";
import { createContext, useCallback, useContext, useState } from "react";
import {
  CONSENT_COOKIE,
  CONSENT_MAX_AGE,
  serializeConsent,
  type Consent,
} from "@/lib/consent/consenso";

type ConsentContextValue = {
  consent: Consent | null;
  haScelto: boolean;
  bannerAperto: boolean;
  external: boolean;
  acceptAll: () => void;
  rejectAll: () => void;
  setExternal: (v: boolean) => void;
  riapriBanner: () => void;
};

const ConsentCtx = createContext<ConsentContextValue | null>(null);

export function useConsent(): ConsentContextValue {
  const ctx = useContext(ConsentCtx);
  if (!ctx) throw new Error("useConsent deve stare dentro <ConsentProvider>");
  return ctx;
}

// Scrive il cookie first-party. encodeURIComponent perché il JSON contiene caratteri
// ({ } " : ,) non ammessi grezzi in un cookie-value.
function scriviCookie(consent: Consent) {
  document.cookie = `${CONSENT_COOKIE}=${encodeURIComponent(
    serializeConsent(consent),
  )}; path=/; max-age=${CONSENT_MAX_AGE}; SameSite=Lax`;
}

export default function ConsentProvider({
  initialConsent,
  children,
}: {
  initialConsent: Consent | null;
  children: React.ReactNode;
}) {
  const [consent, setConsent] = useState<Consent | null>(initialConsent);
  const [bannerAperto, setBannerAperto] = useState<boolean>(initialConsent === null);

  const salva = useCallback((external: boolean) => {
    const nuovo: Consent = { external, ts: Date.now() };
    scriviCookie(nuovo);
    setConsent(nuovo);
    setBannerAperto(false);
  }, []);

  const value: ConsentContextValue = {
    consent,
    haScelto: consent !== null,
    bannerAperto,
    external: consent?.external ?? false,
    acceptAll: useCallback(() => salva(true), [salva]),
    rejectAll: useCallback(() => salva(false), [salva]),
    setExternal: useCallback((v: boolean) => salva(v), [salva]),
    riapriBanner: useCallback(() => setBannerAperto(true), []),
  };

  return <ConsentCtx.Provider value={value}>{children}</ConsentCtx.Provider>;
}
```

- [ ] **Step 3: Scrivere il CookieBanner**

Create `src/components/features/consent/CookieBanner.tsx`:
```tsx
"use client";
import { useState } from "react";
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import Button from "@/components/ui/Button";
import { useConsent } from "./ConsentProvider";

export default function CookieBanner() {
  const t = useTranslations("consent");
  const { bannerAperto, external, acceptAll, rejectAll, setExternal } = useConsent();
  const [personalizza, setPersonalizza] = useState(false);
  const [extScelto, setExtScelto] = useState(external);

  if (!bannerAperto) return null;

  function apriPersonalizza() {
    setExtScelto(external); // sincronizza col valore corrente (utile alla riapertura dal footer)
    setPersonalizza(true);
  }

  return (
    <div className="fixed inset-x-0 bottom-0 z-50 border-t border-white/10 bg-surface-dim/95 backdrop-blur">
      <div className="mx-auto max-w-5xl space-y-4 px-6 py-5">
        <div className="space-y-1">
          <p className="font-display text-sm font-black italic uppercase tracking-tight text-white">
            {t("bannerTitle")}
          </p>
          <p className="font-mono text-[11px] leading-relaxed text-white/60">
            {t("bannerText")}{" "}
            <Link href="/privacy" className="underline hover:text-white">
              {t("linkPrivacy")}
            </Link>{" "}
            ·{" "}
            <Link href="/cookie" className="underline hover:text-white">
              {t("linkCookie")}
            </Link>
          </p>
        </div>

        {personalizza && (
          <div className="space-y-2 border-y border-white/10 py-3">
            <div className="flex items-start justify-between gap-3">
              <div>
                <p className="font-mono text-xs text-white">{t("catNecessary")}</p>
                <p className="font-mono text-[11px] text-white/50">{t("catNecessaryDesc")}</p>
              </div>
              <span className="whitespace-nowrap font-mono text-[10px] uppercase tracking-widest text-white/40">
                {t("alwaysOn")}
              </span>
            </div>
            <label className="flex items-start justify-between gap-3">
              <div>
                <p className="font-mono text-xs text-white">{t("catExternal")}</p>
                <p className="font-mono text-[11px] text-white/50">{t("catExternalDesc")}</p>
              </div>
              <input
                type="checkbox"
                checked={extScelto}
                onChange={(e) => setExtScelto(e.target.checked)}
                className="mt-1"
              />
            </label>
          </div>
        )}

        <div className="flex flex-wrap items-center gap-2">
          {personalizza ? (
            <Button type="button" onClick={() => setExternal(extScelto)}>
              {t("savePrefs")}
            </Button>
          ) : (
            <>
              <Button type="button" onClick={acceptAll}>
                {t("acceptAll")}
              </Button>
              <Button type="button" variant="outline" onClick={rejectAll}>
                {t("rejectAll")}
              </Button>
              <Button type="button" variant="ghost" onClick={apriPersonalizza}>
                {t("customize")}
              </Button>
            </>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Wiring nel root layout**

Modify `src/app/[locale]/layout.tsx`:
(a) Aggiungere gli import:
```ts
import { cookies } from "next/headers";
import { parseConsent, CONSENT_COOKIE } from "@/lib/consent/consenso";
import ConsentProvider from "@/components/features/consent/ConsentProvider";
import CookieBanner from "@/components/features/consent/CookieBanner";
```
(b) Nel corpo della funzione, dopo `const profile = await getProfile();`:
```ts
  const cookieStore = await cookies();
  const rawConsent = cookieStore.get(CONSENT_COOKIE)?.value;
  const initialConsent = parseConsent(rawConsent ? decodeURIComponent(rawConsent) : null);
```
(c) Avvolgere il contenuto dentro `<NextIntlClientProvider>` con `ConsentProvider` e montare `CookieBanner` dopo `<Footer />`:
```tsx
        <NextIntlClientProvider>
          <ConsentProvider initialConsent={initialConsent}>
            <Header isAuthenticated={!!profile} avatar={profile?.avatar_url ?? null} />
            <div className="flex-1 w-full max-w-7xl mx-auto px-6 md:px-12 py-10">{children}</div>
            <Footer />
            <CookieBanner />
          </ConsentProvider>
        </NextIntlClientProvider>
```

- [ ] **Step 5: Verifica + commit**

Run:
```bash
npx tsc --noEmit && npm run lint && npm test
```
Expected: tutto verde. (Il banner si collauda dal vivo nel Task 7.)
```bash
git add src/components/features/consent/ConsentProvider.tsx src/components/features/consent/CookieBanner.tsx "src/app/[locale]/layout.tsx" src/messages/it.json
git commit -m "feat(1d): ConsentProvider + CookieBanner + wiring layout (cookie letto SSR)"
```

---

## Task 3: Gate YouTube (`VideoYouTube`) + integrazione in GalleryEvento

**Files:**
- Create: `src/components/features/consent/VideoYouTube.tsx`
- Modify: `src/components/features/events/GalleryEvento.tsx`

**Interfaces:**
- Consumes: `useConsent()` (Task 2); `estraiIdYouTube` (`src/lib/media/youtube.ts`); i18n `consent`.
- Produces: `VideoYouTube` con props `{ url: string; caption: string | null }`.

- [ ] **Step 1: Scrivere il componente gate**

Create `src/components/features/consent/VideoYouTube.tsx`:
```tsx
"use client";
import { useTranslations } from "next-intl";
import { ExternalLink } from "lucide-react";
import { estraiIdYouTube } from "@/lib/media/youtube";
import { useConsent } from "./ConsentProvider";

export default function VideoYouTube({
  url,
  caption,
}: {
  url: string;
  caption: string | null;
}) {
  const t = useTranslations("consent");
  const { external, setExternal } = useConsent();
  const id = estraiIdYouTube(url);
  if (!id) return null;

  return (
    <div className="space-y-1">
      {external ? (
        <div className="relative w-full overflow-hidden border border-white/10 pt-[56.25%]">
          <iframe
            src={`https://www.youtube-nocookie.com/embed/${id}`}
            title={caption ?? "video"}
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowFullScreen
            className="absolute inset-0 h-full w-full"
          />
        </div>
      ) : (
        <div className="relative w-full overflow-hidden border border-white/10 bg-surface-dim pt-[56.25%]">
          <div className="absolute inset-0 flex flex-col items-center justify-center gap-3 p-4 text-center">
            <p className="font-mono text-[11px] text-white/50">{t("videoBlocked")}</p>
            <button
              type="button"
              onClick={() => setExternal(true)}
              className="border border-white/20 px-4 py-2 font-mono text-xs uppercase tracking-widest text-white transition-colors hover:border-white hover:bg-white/5"
            >
              {t("enableExternal")}
            </button>
            <a
              href={`https://www.youtube.com/watch?v=${id}`}
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center gap-1 font-mono text-[11px] uppercase tracking-widest text-white/40 underline-offset-2 hover:text-white hover:underline"
            >
              <ExternalLink size={12} aria-hidden="true" />
              {t("watchOnYoutube")}
            </a>
          </div>
        </div>
      )}
      {caption && <p className="font-mono text-[11px] text-white/50">{caption}</p>}
    </div>
  );
}
```

- [ ] **Step 2: Sostituire l'iframe inline in GalleryEvento**

Modify `src/components/features/events/GalleryEvento.tsx`.
(a) Rimuovere `estraiIdYouTube` dagli import (ora è dentro `VideoYouTube`) e aggiungere l'import del gate:
```ts
import { ExternalLink, X, ChevronLeft, ChevronRight } from "lucide-react";
import VideoYouTube from "@/components/features/consent/VideoYouTube";
```
*(La riga `import { estraiIdYouTube } from "@/lib/media/youtube";` va RIMOSSA; `ExternalLink`/`X`/`ChevronLeft`/`ChevronRight` restano perché usati altrove.)*
(b) Sostituire l'intero blocco `{video.length > 0 && ( … )}` (l'attuale map con `estraiIdYouTube` + `<iframe>`) con:
```tsx
      {video.length > 0 && (
        <div className="space-y-4">
          {video.map((m) => (
            <VideoYouTube key={m.id} url={m.url} caption={m.caption} />
          ))}
        </div>
      )}
```

- [ ] **Step 3: Verifica + commit**

Run:
```bash
npx tsc --noEmit && npm run lint && npm test
```
Expected: verde.
```bash
git add src/components/features/consent/VideoYouTube.tsx src/components/features/events/GalleryEvento.tsx
git commit -m "feat(1d): gate consenso sugli embed YouTube (VideoYouTube) + integrazione gallery"
```

---

## Task 4: Pagine Privacy Policy e Cookie Policy + i18n

**Files:**
- Create: `src/app/[locale]/(public)/privacy/page.tsx`
- Create: `src/app/[locale]/(public)/cookie/page.tsx`
- Modify: `src/messages/it.json`

**Interfaces:**
- Consumes: next-intl (`getTranslations` server-side).
- Produces: rotte pubbliche `/privacy` e `/cookie`.

- [ ] **Step 1: Aggiungere i namespace `privacy` e `cookiePolicy` a it.json**

Modify `src/messages/it.json` — aggiungere (dopo `consent`):
```json
  "privacy": {
    "title": "Privacy Policy",
    "updated": "Ultimo aggiornamento: luglio 2026",
    "disclaimer": "Bozza non ancora validata da un legale. I testi definitivi verranno pubblicati in seguito. Il documento descrive onestamente le pratiche attuali del sito.",
    "sezioni": [
      {
        "titolo": "1. Titolare del trattamento",
        "paragrafi": [
          "Il titolare del trattamento è Marsica Car Meet [DA COMPILARE: denominazione, sede, email di contatto].",
          "Per qualsiasi richiesta relativa ai tuoi dati puoi scrivere a [DA COMPILARE: email]."
        ]
      },
      {
        "titolo": "2. Dati che raccogliamo",
        "paragrafi": [
          "Account: indirizzo email (necessario per accesso, conferma e reset password).",
          "Profilo: nome, tag, biografia, comune, link ai social, foto avatar — pubblicati volontariamente da te e visibili agli altri membri loggati.",
          "Garage: le auto che aggiungi (marca, modello, anno, foto, dettagli).",
          "Partecipazioni: la tua iscrizione (RSVP) agli eventi e le auto che associ.",
          "Media degli eventi: le foto degli album sono caricate dall'amministratore e sono pubbliche."
        ]
      },
      {
        "titolo": "3. Finalità e basi giuridiche",
        "paragrafi": [
          "Erogazione del servizio (gestione account, profilo, garage, eventi): esecuzione del rapporto con l'utente.",
          "Sicurezza (protezione anti-bot, prevenzione abusi): legittimo interesse.",
          "Caricamento di contenuti di terze parti (video YouTube incorporati): solo previo tuo consenso, revocabile in ogni momento."
        ]
      },
      {
        "titolo": "4. Terze parti e destinatari",
        "paragrafi": [
          "Supabase: hosting del database, autenticazione e archiviazione file.",
          "Google: accesso opzionale con account Google (OAuth), avviato solo se scegli questa modalità.",
          "Cloudflare: protezione anti-bot (Turnstile) e hosting del sito.",
          "YouTube (Google): riproduzione dei video incorporati, caricati solo con il tuo consenso.",
          "Google Drive: alcuni eventi possono avere un link a un archivio Drive con le foto originali. Quell'archivio è esterno al nostro sito e non è governato dai nostri controlli: la rimozione di una foto dal sito non la rimuove da Drive, che va gestito separatamente."
        ]
      },
      {
        "titolo": "5. Conservazione e sicurezza",
        "paragrafi": [
          "I dati sono protetti da controllo d'accesso a livello di database (RLS), autenticazione, cifratura in transito (TLS) e cifratura del disco lato piattaforma.",
          "Non applichiamo cifratura a livello di singola colonna: i dati raccolti oggi sono pubblicati volontariamente in una community e non rientrano tra le categorie particolari; la password non è mai in chiaro (è conservata come hash irreversibile dal provider di autenticazione).",
          "Se in futuro raccoglieremo dati realmente sensibili, valuteremo misure aggiuntive o eviteremo di raccoglierli."
        ]
      },
      {
        "titolo": "6. I tuoi diritti",
        "paragrafi": [
          "Puoi chiedere accesso, rettifica o cancellazione dei tuoi dati.",
          "La cancellazione self-service dell'account sarà disponibile in una fase successiva; nel frattempo puoi richiederla scrivendo a [DA COMPILARE: email].",
          "Hai diritto di proporre reclamo al Garante per la protezione dei dati personali."
        ]
      },
      {
        "titolo": "7. Trasferimenti extra-UE",
        "paragrafi": [
          "Alcuni fornitori (es. Supabase, Google, Cloudflare) potrebbero trattare dati al di fuori dell'UE con garanzie adeguate. [DA VERIFICARE: region e garanzie specifiche una volta configurato l'ambiente di produzione.]"
        ]
      }
    ]
  },
  "cookiePolicy": {
    "title": "Cookie Policy",
    "updated": "Ultimo aggiornamento: luglio 2026",
    "disclaimer": "Bozza non ancora validata da un legale. I testi definitivi verranno pubblicati in seguito.",
    "intro": "I cookie sono piccoli file salvati sul tuo dispositivo. Usiamo solo cookie tecnici necessari e, previo consenso, contenuti di terze parti.",
    "necessaryTitle": "Cookie necessari (sempre attivi)",
    "necessaryRows": [
      "Sessione di accesso (Supabase): mantiene attiva la tua sessione dopo il login.",
      "Preferenza consenso (mcm_consent): ricorda la tua scelta sui cookie, per circa 1 anno.",
      "Sicurezza anti-bot (Cloudflare Turnstile): protegge i moduli di accesso e registrazione."
    ],
    "externalTitle": "Contenuti di terze parti (solo con consenso)",
    "externalText": "I video di YouTube incorporati negli album degli eventi vengono caricati solo se acconsenti. Finché non acconsenti, al loro posto vedi un segnaposto e nessun cookie di YouTube viene impostato.",
    "noAnalytics": "Non usiamo cookie analitici né di marketing.",
    "manageTitle": "Gestire il consenso",
    "manageText": "Puoi cambiare idea in ogni momento dal link \"Preferenze cookie\" in fondo a ogni pagina: riapre il pannello e ti permette di attivare o revocare i contenuti di terze parti."
  }
```

- [ ] **Step 2: Scrivere la pagina Privacy**

Create `src/app/[locale]/(public)/privacy/page.tsx`:
```tsx
import { getTranslations } from "next-intl/server";

export async function generateMetadata() {
  const t = await getTranslations("privacy");
  return { title: `${t("title")} — Marsica Car Meet` };
}

type Sezione = { titolo: string; paragrafi: string[] };

export default async function PrivacyPage() {
  const t = await getTranslations("privacy");
  const sezioni = t.raw("sezioni") as Sezione[];

  return (
    <article className="mx-auto max-w-3xl space-y-8">
      <header className="space-y-2">
        <h1 className="font-display text-3xl font-black italic uppercase tracking-tighter text-white">
          {t("title")}
        </h1>
        <p className="font-mono text-[11px] text-white/40">{t("updated")}</p>
        <p className="border border-accent-red/40 bg-accent-red/10 p-3 font-mono text-[11px] text-white/70">
          {t("disclaimer")}
        </p>
      </header>
      {sezioni.map((s) => (
        <section key={s.titolo} className="space-y-2">
          <h2 className="font-mono text-sm uppercase tracking-widest text-white">{s.titolo}</h2>
          {s.paragrafi.map((p, i) => (
            <p key={i} className="text-sm leading-relaxed text-white/70">
              {p}
            </p>
          ))}
        </section>
      ))}
    </article>
  );
}
```

- [ ] **Step 3: Scrivere la pagina Cookie**

Create `src/app/[locale]/(public)/cookie/page.tsx`:
```tsx
import { getTranslations } from "next-intl/server";

export async function generateMetadata() {
  const t = await getTranslations("cookiePolicy");
  return { title: `${t("title")} — Marsica Car Meet` };
}

export default async function CookiePage() {
  const t = await getTranslations("cookiePolicy");
  const necessaryRows = t.raw("necessaryRows") as string[];

  return (
    <article className="mx-auto max-w-3xl space-y-8">
      <header className="space-y-2">
        <h1 className="font-display text-3xl font-black italic uppercase tracking-tighter text-white">
          {t("title")}
        </h1>
        <p className="font-mono text-[11px] text-white/40">{t("updated")}</p>
        <p className="border border-accent-red/40 bg-accent-red/10 p-3 font-mono text-[11px] text-white/70">
          {t("disclaimer")}
        </p>
      </header>

      <p className="text-sm leading-relaxed text-white/70">{t("intro")}</p>

      <section className="space-y-2">
        <h2 className="font-mono text-sm uppercase tracking-widest text-white">
          {t("necessaryTitle")}
        </h2>
        <ul className="space-y-1">
          {necessaryRows.map((r, i) => (
            <li key={i} className="text-sm leading-relaxed text-white/70">
              • {r}
            </li>
          ))}
        </ul>
      </section>

      <section className="space-y-2">
        <h2 className="font-mono text-sm uppercase tracking-widest text-white">
          {t("externalTitle")}
        </h2>
        <p className="text-sm leading-relaxed text-white/70">{t("externalText")}</p>
        <p className="text-sm leading-relaxed text-white/50">{t("noAnalytics")}</p>
      </section>

      <section className="space-y-2">
        <h2 className="font-mono text-sm uppercase tracking-widest text-white">{t("manageTitle")}</h2>
        <p className="text-sm leading-relaxed text-white/70">{t("manageText")}</p>
      </section>
    </article>
  );
}
```

- [ ] **Step 4: Verifica + commit**

Run:
```bash
npx tsc --noEmit && npm run lint && npm test
```
Expected: verde.
```bash
git add "src/app/[locale]/(public)/privacy/page.tsx" "src/app/[locale]/(public)/cookie/page.tsx" src/messages/it.json
git commit -m "feat(1d): pagine Privacy e Cookie Policy (bozza IT + disclaimer)"
```

---

## Task 5: Link nel footer + PreferenzeCookieButton

**Files:**
- Create: `src/components/features/consent/PreferenzeCookieButton.tsx`
- Modify: `src/components/layout/Footer.tsx`

**Interfaces:**
- Consumes: `useConsent()` (Task 2); rotte `/privacy` e `/cookie` (Task 4); i18n `footer`.
- Produces: nulla (foglia).

- [ ] **Step 1: Scrivere il bottone client**

Create `src/components/features/consent/PreferenzeCookieButton.tsx`:
```tsx
"use client";
import { useTranslations } from "next-intl";
import { useConsent } from "./ConsentProvider";

export default function PreferenzeCookieButton() {
  const t = useTranslations("footer");
  const { riapriBanner } = useConsent();
  return (
    <button
      type="button"
      onClick={riapriBanner}
      className="uppercase tracking-widest transition-colors hover:text-white"
    >
      {t("cookiePrefs")}
    </button>
  );
}
```

- [ ] **Step 2: Aggiungere i link al footer**

Modify `src/components/layout/Footer.tsx` — sostituire il contenuto con:
```tsx
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import PreferenzeCookieButton from "@/components/features/consent/PreferenzeCookieButton";

export default function Footer() {
  const t = useTranslations("footer");
  const tb = useTranslations("brand");
  return (
    <footer className="mt-auto bg-surface-dim border-t border-white/5 py-6 px-6 md:px-12">
      <div className="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center gap-3 text-[10px] font-mono text-white/40">
        <span>{t("copyright")}</span>
        <nav className="flex items-center gap-4">
          <Link href="/privacy" className="uppercase tracking-widest transition-colors hover:text-white">
            {t("privacy")}
          </Link>
          <Link href="/cookie" className="uppercase tracking-widest transition-colors hover:text-white">
            {t("cookie")}
          </Link>
          <PreferenzeCookieButton />
        </nav>
        <span className="uppercase tracking-widest text-accent-red">{tb("payoff")}</span>
      </div>
    </footer>
  );
}
```

- [ ] **Step 3: Verifica + build + commit**

Run (⚠️ NON mentre gira `next dev`):
```bash
npx tsc --noEmit && npm run lint && npm test && rm -rf .next && npm run build
```
Expected: tutto verde.
```bash
git add src/components/features/consent/PreferenzeCookieButton.tsx src/components/layout/Footer.tsx
git commit -m "feat(1d): footer con link Privacy/Cookie + Preferenze cookie (riapre banner)"
```

---

## Task 6: Review finale whole-branch + wave di fix

**Files:** eventuali fix su tutti i file della fase.

- [ ] **Step 1: Review indipendente dell'intero branch**

Usare `superpowers:requesting-code-review` sul diff `main..feat/fase1d-gdpr` (modello capace). Ambiti da controllare in particolare:
- **Nessun hydration mismatch**: `bannerAperto` e il gate `VideoYouTube` derivano da `initialConsent` (server) al primo paint; il cookie è letto SSR e passato al provider.
- **Encode/decode del cookie coerente**: client scrive `encodeURIComponent`, server legge `decodeURIComponent`; `parseConsent` opera sul JSON decodificato.
- **GDPR**: "Rifiuta" prominente quanto "Accetta"; banner non bloccante; revoca dal footer funzionante; nessuna terza parte caricata prima del consenso (l'iframe YouTube non è nel DOM finché `external` è falso).
- **`useConsent` sempre dentro il provider** (banner, VideoYouTube, PreferenzeCookieButton sono tutti figli del provider nel layout).
- **`t.raw` tipizzato** nelle pagine policy (cast a tipo esplicito) e nessuna chiave i18n mancante.
- **`Date.now()`** usato solo lato client (nel provider), non in codice che gira in fase di build statica.

- [ ] **Step 2: Applicare una sola wave di fix**

Raccogliere Minor/Important in un'unica tornata. Riverificare: `npx tsc --noEmit && npm run lint && npm test`.

- [ ] **Step 3: Commit finale della wave**

```bash
git add -A
git commit -m "fix(1d): wave di fix dalla review finale whole-branch"
```

---

## Task 7: Collaudo dal vivo e chiusura fase

**Files:** eventuali fix dei bug emersi (commit dedicati).

Richiede l'ambiente acceso (Docker + `npx supabase start` + `npm run dev` su **localhost:3000**). Un evento **concluso con almeno un video** serve per provare il gate (es. `prova-primo-evento`).

- [ ] **Step 1: Banner e persistenza**
  - Con cookie `mcm_consent` assente (finestra anonima / cancella cookie) → **il banner appare** in basso su ogni pagina.
  - **Accetta tutti** → banner sparisce; ricaricando **non riappare** (letto SSR, nessun flash).
  - Cancella il cookie, ricarica → banner riappare. **Rifiuta non essenziali** → sparisce, cookie `external:false`.
  - **Personalizza** → pannello con "Necessari" (sempre attivi) + toggle "Contenuti di terze parti"; **Salva preferenze** scrive la scelta.

- [ ] **Step 2: Gate YouTube**
  - Su un evento concluso con video, **da consenso negato/assente** → al posto dell'iframe il **placeholder** con "Attiva contenuti esterni" e "Guarda su YouTube".
  - **"Attiva contenuti esterni"** → l'iframe compare subito e gli **altri** video della pagina si sbloccano; il cookie diventa `external:true`.
  - **"Guarda su YouTube"** → apre `youtube.com` in nuova scheda.
  - Verifica che, a consenso negato, **nessuna richiesta a youtube-nocookie.com** parta (Network tab): l'iframe non è nel DOM.

- [ ] **Step 3: Revoca dal footer**
  - Link **"Preferenze cookie"** in fondo → riapre il banner; togli il consenso ai contenuti esterni e salva → i video **tornano placeholder** senza reload.

- [ ] **Step 4: Pagine policy**
  - `/it/privacy` e `/it/cookie` visibili **da sloggato**, con il **disclaimer** in cima; i link del footer ci arrivano; `<title>` corretto.

- [ ] **Step 5: Verifiche standard**

Run (⚠️ NON mentre gira `next dev`):
```bash
npm test && npx tsc --noEmit && npm run lint && rm -rf .next && npm run build
```
Expected: tutto verde.

- [ ] **Step 6: Correggere i bug emersi** (commit dedicati) e **chiudere la fase**

Usare `superpowers:finishing-a-development-branch`: merge di `feat/fase1d-gdpr` su `main`, elimina il branch. Aggiornare `docs/STATO-LAVORI.md` (1D chiusa → **Fase 1 / MVP completa**) e, se si tiene aggiornato, spuntare le checkbox 1C/1D nel `ROADMAP.md`.

---

## Self-Review (fatta in fase di stesura)

**Spec coverage:**
- Cookie banner accetta/rifiuta/personalizza → Task 2. ✅
- Consenso letto SSR (no flash) → Task 2 (layout + provider `initialConsent`). ✅
- Gate YouTube (placeholder + attiva + guarda su YouTube) → Task 3. ✅
- Due categorie oneste (Necessari + Esterni) → Task 2 (banner) + Task 4 (cookie policy). ✅
- Pagine Privacy/Cookie con bozza IT + disclaimer + caveat D-172 + D-173 → Task 4. ✅
- Footer con Privacy/Cookie/Preferenze cookie (revoca) → Task 5. ✅
- Logica pura testata → Task 1. ✅
- Nessun DB/migrazione → rispettato (nessun task tocca `supabase/`). ✅
- Cancellazione account = Fase 2 → solo menzionata nel testo privacy, non implementata. ✅

**Placeholder scan:** i `[DA COMPILARE]`/`[DA VERIFICARE]` sono **contenuti** intenzionali delle policy (dati del Titolare/region), non buchi del piano; ogni step di codice ha codice reale.

**Type consistency:** `Consent = {external, ts}`, `parseConsent`/`serializeConsent` coerenti tra Task 1 (def) e Task 2 (uso). `useConsent()` shape coerente tra Task 2 (def) e i consumatori Task 3/5. `VideoYouTube({url, caption})` coerente tra Task 3 (def) e GalleryEvento (uso: `m.url`, `m.caption`). `t.raw("sezioni") as Sezione[]` / `t.raw("necessaryRows") as string[]` coerenti con le forme JSON del Task 4 Step 1.
